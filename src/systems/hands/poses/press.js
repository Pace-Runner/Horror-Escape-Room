/**
 * press - index extended and stiffened for a button.  [owner: Hands]
 *
 * Not the same as `point`: pressing straightens the index further and drops the
 * wrist so the fingertip leads the motion. `point` gestures at something,
 * `press` applies force to it. The breaker panel, the containment control and
 * the CCTV channel buttons all read better for the difference.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const press = Object.freeze({
  // Unit 4: author here.
});
