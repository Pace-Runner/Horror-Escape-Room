/**
 * grip-flat - fingers closed around a thin flat object.  [owner: Hands]
 *
 * The cassette tape and the crowbar's flat end. Distinct from `grip-cylinder`
 * because the fingers close much further before they meet resistance, and the
 * thumb opposes flat rather than wrapping - a cassette held in a cylinder grip
 * looks like it is being held by a hand expecting a torch.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const gripFlat = Object.freeze({
  // Unit 4: author here.
});
