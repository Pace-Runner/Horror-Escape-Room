/**
 * reach - hand opening toward something usable.  [owner: Hands]
 *
 * This pose is AFFORDANCE FEEDBACK, not decoration: it is how the game says
 * "this object is usable" without putting a UI prompt on screen. The fingers
 * spread and the wrist rotates slightly palm-down, as a hand does a moment
 * before it takes hold of something.
 *
 * Wider than `open` and less symmetrical - the index and middle lead, the ring
 * and pinky trail. A hand that reaches with all four fingers in unison reads as
 * a claw.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const reach = Object.freeze({
  // Unit 4: author here.
});
