/**
 * brace - flat palm, fingers together, taking weight.  [owner: Hands]
 *
 * A palm against a wall while running a corner, and the settle pose at the end
 * of `push-door`. The fingers splay slightly under load and the wrist extends
 * back - a perfectly flat hand with the wrist neutral reads as a mime pressing
 * an invisible window rather than as weight going through an arm.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const brace = Object.freeze({
  // Unit 4: author here.
});
