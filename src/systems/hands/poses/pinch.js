/**
 * pinch - thumb tip meeting index tip.  [owner: Hands]
 *
 * Holds the key, the paperclip and the photograph. The acceptance bar from the
 * brief is exact: the two tips must come into CONTACT without interpenetrating.
 * A visible gap reads as the object floating; overlap reads as the geometry
 * being broken. This is the pose to author last, after the segment radii in
 * hand-mesh.js have settled, because it is the one that depends on them.
 *
 * The `pinch` socket parents to index.03 - see sockets.js for why.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const pinch = Object.freeze({
  // Unit 4: author here.
});
