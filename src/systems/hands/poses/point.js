/**
 * point - index extended, remaining fingers curled.  [owner: Hands]
 *
 * Used for `press-button` and `flip-switch`, and as the approach pose for
 * `lockpick`. The thumb should fold across the curled middle finger rather than
 * sticking out sideways - a thumb left at rest in a pointing hand reads as a
 * finger gun.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const point = Object.freeze({
  // Unit 4: author here.
});
