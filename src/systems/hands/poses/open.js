/**
 * open - fingers extended and slightly splayed.  [owner: Hands]
 *
 * The release end of `drop`, and the offer end of `hold-flat`. Splay matters:
 * fingers extended but perfectly parallel read as a signal, not as a hand
 * letting go of something.
 *
 * Unit 4 authors the values. Format: joint -> [x, y, z] Euler in radians.
 */

export const open = Object.freeze({
  // Unit 4: author here.
});
