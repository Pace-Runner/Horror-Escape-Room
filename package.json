{
  "name": "dont-let-it-out",
  "version": "0.1.0",
  "private": true,
  "description": "Don't Let It Out - horror escape room (Three.js, CGV group project)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check:maze": "node scripts/check-maze.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
